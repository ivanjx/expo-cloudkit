name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  transport:
    name: Expo 57 package and native verification
    uses: ./.github/workflows/transport.yml
